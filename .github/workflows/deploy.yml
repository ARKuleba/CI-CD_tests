name: CI/CD Pipeline

on:
  push:
    branches: [main]  # ТОЛЬКО main
  workflow_dispatch: # для ручного запуска деплоев

env:
  NODE_ENV: test

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Cache node modules
      uses: actions/cache@v3
      with:
        path: node_modules
        key: ${{ runner.os }}-node-${{ hashFiles('package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-node-
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run unit tests
      run: npm test
      continue-on-error: false
    
    - name: Run integration tests
      run: npm run test:integration
      continue-on-error: false
    
    - name: Upload test results
      uses: actions/upload-artifact@v3
      if: always()
      with:
        name: test-results
        path: |
          test-results/
          coverage/
        retention-days: 7

  deploy-staging:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'  # РУЧНОЙ ЗАПУСК
    environment:
      name: staging
      url: ${{ secrets.RENDER_STAGING_URL }}
    
    steps:
    - name: Deploy to Staging (Render)
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_STAGING_SERVICE_ID: ${{ secrets.RENDER_STAGING_SERVICE_ID }}
      run: |
        echo "🚀 Deploying to Staging on Render..."
        curl -X POST \
          "https://api.render.com/v1/services/$RENDER_STAGING_SERVICE_ID/deploys" \
          -H "Authorization: Bearer $RENDER_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"clearCache": true}'
        echo "✅ Staging deployment triggered"

  staging-smoke-test:
    needs: deploy-staging
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    
    steps:
    - name: Install curl
      run: sudo apt-get update && sudo apt-get install -y curl
    
    - name: Smoke test Staging
      env:
        STAGING_URL: ${{ secrets.RENDER_STAGING_URL }}
      run: |
        echo "🧪 Testing Staging Environment..."
        echo "Staging URL: $STAGING_URL"
        
        # Простая проверка (упрощённая)
        response=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$STAGING_URL/health" || echo "000")
        
        if [ "$response" = "200" ]; then
          echo "✅ Staging is up and running (HTTP $response)"
          exit 0
        else
          echo "❌ Staging failed: HTTP $response"
          exit 1
        fi

  deploy-production:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    environment:
      name: production
      url: ${{ secrets.RENDER_PRODUCTION_URL }}
    
    steps:
    - name: Deploy to Production (Render)
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_PRODUCTION_SERVICE_ID: ${{ secrets.RENDER_PRODUCTION_SERVICE_ID }}
      run: |
        echo "🚀 Deploying to Production on Render..."
        curl -X POST \
          "https://api.render.com/v1/services/$RENDER_PRODUCTION_SERVICE_ID/deploys" \
          -H "Authorization: Bearer $RENDER_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"clearCache": true}'
        echo "✅ Production deployment triggered"

  production-smoke-test:
    needs: deploy-production
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    
    steps:
    - name: Install curl
      run: sudo apt-get update && sudo apt-get install -y curl
    
    - name: Smoke test Production
      env:
        PRODUCTION_URL: ${{ secrets.RENDER_PRODUCTION_URL }}
      run: |
        echo "🧪 Testing Production Environment..."
        echo "Production URL: $PRODUCTION_URL"
        
        # Простая проверка
        response=$(curl -s -o /dev/null -w "%{http_code}" --max-time 10 "$PRODUCTION_URL/health" || echo "000")
        
        if [ "$response" = "200" ]; then
          echo "✅ Production is up and running (HTTP $response)"
          exit 0
        else
          echo "❌ Production failed: HTTP $response"
          exit 1
        fi

  smart-rollback:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    environment:
      name: production
    
    steps:
    - name: Install dependencies
      run: sudo apt-get update && sudo apt-get install -y curl jq
    
    - name: Smart rollback procedure
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_PRODUCTION_SERVICE_ID: ${{ secrets.RENDER_PRODUCTION_SERVICE_ID }}
      run: |
        echo "🎯 Smart rollback procedure..."
        
        # 1. Проверяем историю деплоев
        echo "📋 Checking deploy history..."
        DEPLOYS_JSON=$(curl -s \
          "https://api.render.com/v1/services/$RENDER_PRODUCTION_SERVICE_ID/deploys?limit=5" \
          -H "Authorization: Bearer $RENDER_API_KEY")
        
        SUCCESSFUL_DEPLOYS=$(echo "$DEPLOYS_JSON" | jq -r '[.[] | select(.status == "succeeded")] | length')
        
        if [ "$SUCCESSFUL_DEPLOYS" -gt 1 ]; then
          echo "✅ Found $SUCCESSFUL_DEPLOYS successful deploys - using Render rollback"
          
          PREVIOUS_DEPLOY_ID=$(echo "$DEPLOYS_JSON" | jq -r '
            [.[] | select(.status == "succeeded")] | 
            if length > 1 then .[1].id else null end
          ')
          
          echo "🔄 Rolling back to deploy ID: $PREVIOUS_DEPLOY_ID"
          
          curl -X POST \
            "https://api.render.com/v1/services/$RENDER_PRODUCTION_SERVICE_ID/deploys" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"deployId\": \"$PREVIOUS_DEPLOY_ID\"}"
            
          echo "✅ Render rollback initiated"
          
        else
          echo "⚠️  Not enough deploy history - using simple redeploy"
          
          curl -X POST \
            "https://api.render.com/v1/services/$RENDER_PRODUCTION_SERVICE_ID/deploys" \
            -H "Authorization: Bearer $RENDER_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"clearCache": true}'
            
          echo "✅ Simple redeploy initiated"
        fi

  simple-redeploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    environment:
      name: production
    
    steps:
    - name: Install curl
      run: sudo apt-get update && sudo apt-get install -y curl
    
    - name: Simple redeploy to production
      env:
        RENDER_API_KEY: ${{ secrets.RENDER_API_KEY }}
        RENDER_PRODUCTION_SERVICE_ID: ${{ secrets.RENDER_PRODUCTION_SERVICE_ID }}
      run: |
        echo "🔄 Simple redeploy to production..."
        
        RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
          "https://api.render.com/v1/services/$RENDER_PRODUCTION_SERVICE_ID/deploys" \
          -H "Authorization: Bearer $RENDER_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"clearCache": true}')
        
        BODY=$(echo "$RESPONSE" | head -n -1)
        STATUS=$(echo "$RESPONSE" | tail -n1)
        
        if [ "$STATUS" -eq 200 ] || [ "$STATUS" -eq 201 ]; then
          echo "✅ Production redeploy triggered successfully"
          echo "This will deploy the latest stable commit on main branch"
          echo "Response: $BODY"
        else
          echo "❌ Failed to trigger redeploy: HTTP $STATUS"
          echo "Response: $BODY"
          exit 1
        fi