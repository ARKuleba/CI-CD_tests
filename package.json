{
  "name": "lab4_test",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "set NODE_ENV=test&& jest --testPathPattern=unit --verbose --runInBand",
    "test:integration": "set NODE_ENV=test&& jest --testPathPattern=integration --verbose --runInBand",
    "test:simple": "set NODE_ENV=test&& jest test/integration/simple-auth.test.js --verbose",
    "test:all": "set NODE_ENV=test&& jest --verbose --runInBand",
    "test:ci": "set NODE_ENV=test&& jest --ci --reporters=default --reporters=jest-junit --runInBand"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.15.3",
    "pg": "^8.11.3",
    "pg-connection-string": "^2.9.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^6.0.3",
    "jest": "^29.5.0",
    "jest-junit": "^16.0.0",
    "nodemon": "^2.0.22",
    "sqlite3": "^5.1.7",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
